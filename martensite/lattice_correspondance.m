function funcs = lattice_correspondance[ transformed_directions ]
% based = transform_directions(vectorlist ,vectors, direction )
% transform a list of directions from initial phase
% to final phase based on lattice correspondence

    function express_basis(A,B)
    % given a matrix A of initial basis vectors (as colums) 
    % and another such basis matrix B calculates the transformation matrix between them
    
(2* eye(3)) \ eye(3)
    end


lattice_correspondence =
list_of_directions =
list_of_planes =

if dircetion == 'toInitial'
end
    
if direction == 'toParent'
   end
   
if vectors == 'planes'
   end
   
if vectors == 'directions'
end
  
end
    